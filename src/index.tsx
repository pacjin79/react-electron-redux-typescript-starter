import * as React from 'react';
import * as ReactDOM from 'react-dom';

const root = <h1>I love minecraft  me too!</h1>;

ReactDOM.render(
    root,
    document.getElementById("bootstrapContainer")
);